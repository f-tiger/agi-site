# 工具的付费能力:先量自己,再谈收费(2026-09-16)

owner 原话:「先优化prompt再执行:从工具角度看，哪些工具最容易被付费？然后看看调研做到子站点」。

本文回答两问:①**哪一类工具最容易被付费**(框架 + 外部证据 + 舰队自查);②**要不要为它开子站点**(裁定:不开,
理由与替代动作在 §五)。所有第一方数字为 **2026-09-16 D1 现查**,SQL 写在 §一末尾,任何后续会话可复算。

---

## 〇、三轮 prompt(按 owner 要求先优化再执行)

**第一轮(字面)**:「哪些工具最容易被付费?」→ 会得到一张品类清单(PDF 工具、报税、SEO、图片处理……)。
这张清单**没有决策价值**:品类的付费率是别人站上的读数,搬到一个每天几十个真人的站上仍然是 0。
08-31「零读者不装转化件」和 09-14「按流量计价的通道在此体量都是 €0–5/月」已经把这条路走过两遍。

**第二轮(把问题换成可判定的)**:付费不是**工具品类**的属性,是**使用那一刻**的属性。同一个「计算器」,
放在「今晚要交的申报表」旁边有人付钱,放在「随便算算」旁边没人付钱。所以要问的是:
**这一次使用,是谁在什么压力下、花谁的钱、为了交出什么东西?** 由此得到五个触发器与一条否决门(§二)。

**第三轮(可执行,带舰队约束)**:在回答「哪类工具能收费」之前,**先量舰队自己的工具面到底有没有人用**。
若使用量本身趋近于 0,那么「选哪个工具收费」就是个假问题——这正是本轮的读数结论(§一:28 天 **61 次**)。
因此本轮的产出定为:**框架 + 自查 + 裁定 + 把「工具值不值得做」这件事变成一条会到期的判定线**,
而不是再建一个页面或一个站。

---

## 一、先量自己:舰队工具面现况(D1 现查 2026-09-16)

### 1. 工具页有多少

| 站 | 带 `WebApplication` 的页 | 说明 |
|---|---|---|
| agiscorecard | 12 | 含 zh 镜像 |
| getecoback | 12 | 德/英 计算器 |
| thedollscout | 12 | en/de/zh/th 四语,5 个真工具 |
| gridlings | 32 | **游戏,不计入工具面** |
| after35 / fanzha / codeword / firstjob / learn / powerbill | 4/3/3/3/2/2 = 17 | 09-14、09-15 新建 |
| buysomething(SR) | 0(未打 WebApplication 标)| 有 landed-cost 等计算件 |

**非游戏工具页合计 53 张。**

### 2. 28 天里被用了多少次(只数读者主动发起的,剔 CI)

| 站 | 事件 | 28 天次数 |
|---|---|---|
| agiscorecard | `calc_use` | **21**(只落在 4 天) |
| getecoback | `btu_calc` 19 + `standort_check` 3 + `seal_fit` 1 | **23** |
| buysomething | `calc_use` | **13**(4 天) |
| baipiaoji | `calc` | **4** |
| thedollscout | `checker_use`/`odds_calc`/`finder_use`/`lookup_use`/`cost_calc` | **0**(且自 08-30 改版起**一次都没有过**) |
| after35 / fanzha / codeword / firstjob / learn / powerbill | `tool_result`/`calc_run`/`path_result` | **0** |

**舰队合计:61 次 / 28 天 ≈ 2.2 次/天,分摊到 53 张工具页 = 每页每月 1.2 次。**

三条读数纪律,防止把这张表读错:
1. **eco 的 `heat_now` 135、`strom_now` 23 没有计入**——它们是页面上的实时小部件**随页面加载自动打点**,
   不是读者主动使用。把它们算进来会让工具面看起来热闹 3 倍,这正是自己骗自己的典型做法。
2. **eco 的 `lead_intent` 10 与 `cold_now` 113 全部来自 `/__ci_healthcheck`**(meta `{"ci":1}`),真人 = 0。
3. **六个新站的 0 是「真的没人用」,不是「没装探针」**——13 张工具页逐页核对,`fzev/cwev/jbev/lxev/pbev`
   的 `tool_result`/`calc_run` 调用都在;after35 四张工具页也在发 `path_result`。
   但六站的 `ua_class='js'` 行数**全部为 0**:上线以来没有任何一次客户端事件(工具、分享、FAQ 展开都没有),
   说明能跑 JS 的真人互动次数是 0,而服务端按 UA 记的 "human" pv(158–393)里几乎没有真读者。
   **本轮为此补了信标真值测试(§六),把「没人用」和「管子断了」这两件长得一样的事分开。**

### 3. 已经建好、但一次都没卖出去的收款面

| 面 | 状态(D1 现查) |
|---|---|
| bpj 付费 listing(`ads` / `ad_orders` 两张表) | **0 行 / 0 行** |
| SR 付费 Opportunity Packs | 链路已建,Stripe 五个值未设 → 未开售;`/packs` 无任何事件 |
| agi `/audits` $499 · `/advertise` $99 | 28 天服务端 human pv:`/advertise` 60、`/audits` 44 + `/zh/audits` 22 → **询单 0** |
| bpj `watches`(免费/付费 tier 的变更告警钩子) | **0 行** |

**这一格是本轮最该被记住的事实:舰队已经建过四个收款面,卖出 0 单。** 所以「哪个工具容易收费」如果被理解成
「再建第五个收款面」,答案一定还是 0。绑定约束不在工具品类。

### 4. 唯一每天都有第三方来消费的工具面:机器面

| 端点 | 28 天调用 | 谁在调 |
|---|---|---|
| bpj `/api/changes` | 394 次 / 29 天 | 其中 **135 次是本仓 deploy 自检**(curl/8.5.0);剩下有一个 SG 的定时调用方 **77 次 / 26 天**、Mac Chrome UA 27 次 / 25 天,以及 Amazonbot 29、AIWebIndex 12、PerplexityBot 5、bingbot 5、YandexBot 6 |
| bpj `/api/mcp/*`(11 个工具/资源端点) | 约 360 次 | **SaSame-MCP-Audit/0.1(DE)23 次 / 22 天**、**rokmcp-collector/0.2(FI)12 次 / 12 天**,其余为自检 |
| eco `/mcp` | `mcp_call` 105 次 / 5 天 | 同样两个采集器 + `node` UA,**参数全部是端点示例里的那几组**(典型探针形状) |

**读法**:这是舰队里唯一「没做任何推广、第三方每天自己回来」的工具面。但要诚实:**回来的是索引器/采集器,
不是买家**——它们在给 MCP 目录建索引。所以它是**需求形状**的证据,不是**付费**的证据(判定线见 §七)。

### 复算 SQL(任何后续会话照抄)

```sql
-- agiscorecard-events
SELECT name, COUNT(*) FROM events WHERE day >= date('now','-28 days') AND name='calc_use' GROUP BY name;
-- ecoback-events(剔 CI 页)
SELECT name, COUNT(*) FROM ev WHERE day >= date('now','-28 days')
  AND name IN ('btu_calc','standort_check','seal_fit') AND page NOT LIKE '%__ci%' GROUP BY name;
-- sourceradar-events
SELECT name, COUNT(*) FROM ev WHERE day >= date('now','-28 days') AND name='calc_use' GROUP BY name;
-- baipiaoji-hits
SELECT ev, COUNT(*) FROM hits WHERE d >= date('now','-28 days') AND ev IN ('calc','sheet_dl','go') GROUP BY ev;
-- dollscout-events
SELECT ev, COUNT(*) FROM hits WHERE ev IN ('checker_use','odds_calc','finder_use','lookup_use','cost_calc') GROUP BY ev;
-- after35-events(六站共用;label='__ci' 是部署自检写的,必须剔)
SELECT 'fanzha', name, COUNT(*) FROM fev WHERE ua_class='js' AND label<>'__ci' GROUP BY name;  -- cev/jev/lev/pev/ev 同理
```

---

## 二、框架:付费的五个触发器 + 一条否决门

一个工具**容易被付费**,不取决于它多聪明,取决于使用那一刻是否踩中下面的触发器。**踩中 0–1 条的,
永远不要在上面装收款**(这正是舰队 53 张工具页里绝大多数的位置)。

| # | 触发器 | 判据(一句话) | 例子 |
|---|---|---|---|
| ① | **交付物** | 输出是**别人要求**的文件/证明,不是屏幕上的一个数 | 报税申报、合规报告、可提交的表格 |
| ② | **期限** | 有外部截止日,错过要罚钱或失权 | 申报期、保修期、退货窗口 |
| ③ | **别人的钱包** | 买单的是公司/可报销,不是消费者的可选支出 | 卖家工具、开发者工具 |
| ④ | **持续状态** | 价值随时间累积:监控、告警、留痕;一次算完就归零的不算 | 价格/额度变更告警、召回雷达 |
| ⑤ | **责任转移** | 算错要赔或要复核,买的是背书不是算术 | 关税/合规口径、带出处的核算 |

**否决门(先过这一关再谈触发器)**:**这件事能不能被 ChatGPT 一句话答完,或者 view-source 之后自己跑?**
能 → 永远不会被付费。舰队的工具**全部是「不上传的纯前端工具」**(这是新站流水线的硬规定,也是隐私上的优点):
前端工具的逻辑对任何人都是公开的,**结构上就没有可收费的东西**。能收费的只能是**服务端持有的东西**:
数据、时间序列、告警、可交付文件、责任承诺。

**AI 时代把否决门抬高了**(外部证据见 §三):简单计算/换算/判定类的需求正在被助手直接吃掉,
零点击率上升、AI Overviews 覆盖面扩大,而这些工具的价值恰恰是「省一次搜索」。
**结论:纯前端判定型小工具在 2026 年是内容与引用资产,不是营收资产。**

---

## 三、外部证据(全部带日期;标明一手/二手)

| 证据 | 数字与日期 | 类型 | 对本裁定的作用 |
|---|---|---|---|
| RevenueCat《State of Subscription Apps 2026》 | Utilities 类下载→试用中位 **6.5%**;Utilities 试用用户 12 个月 LTV **$68.90**(该报告里最高);Productivity Day-0 转化 **71.9%** | 厂商年度报告(样本为移动订阅 app,不是网页工具) | 工具**能**赚钱,但赚的是**订阅制、有分发渠道**的那一类 |
| 同报告 / 行业基准 | 硬付费墙 Day-35 试用→付费中位 **10.7%** vs freemium **2.1%**;无卡试用平均 **8.9%**、绑卡试用 **31.4%**(2026 数据) | 二手汇总 | 免费工具→付费的转化在个位数;61 次/28 天的使用量乘以任何一个数都 <1 单 |
| 电商大盘转化 | 全球电商转化 **1.6–3.5%**,2025 Q3 均值 **2.5%** | 二手汇总 | 同上:一次性付费件需要**千级**月使用量才谈得上一单 |
| SparkToro(2026) | 2026 年**不到三分之一**的 Google 搜索还会产生点击;美国零点击率 **68.01%**(2024 年 60.45%) | 一手研究(该公司自有点击流分析) | 「工具页靠搜索捡流量」这条路在收窄 |
| AI Overviews 覆盖 | 出现在约 **48%** 的搜索(2026-03,较 2025-12 的 ~34% 上升);出现时自然结果 CTR 降约 **61%** | 二手汇总 | 判定型/计算型问题正是 AIO 最擅长直接答的 |
| Dictionary.com 访问量 | 3 410 万 → **2 130 万**(2025 年) | 二手汇总 | 「查一下就走」的工具站是 AI 助手的第一批受害者 |
| 德国房产税申报(2022–2023) | 涉及约 **3 600 万**业主;申报期压到 **2023-01-31**;smartsteuer 等在线工具首份 **€34,95**、每份追加 €29,95 | 厂商定价页 + 公开时间线 | **①交付物 + ②期限 + ⑤责任**三条同时成立时,消费者**真的**会为一个网页工具付 €35 |
| Keepa | Pro **€19–29/月**(不同来源口径不一致,**本文不挑一个当定论**),免费档长期存在 | 厂商/评测站 | **④持续状态**能收费:价格历史与降价告警 |
| Visualping | 个人付费档约 **$14/月**起,Business **$100/月**起;免费 150 次/月 | 厂商定价页 | 同上:变更监控是能收费的形态 |
| MCP 变现现状(2026) | 「**不到 5%** 的 MCP server 产生任何收入」;全球 agent→tool 支付量「**低于 $50k/天**」;x402 / Stripe MPP(2026-03)等计费管道在 2026 年才铺开 | **厂商博客口径,未见一手统计,按线索对待** | 机器面付费在**技术上**可行、在**市场上**还没有规模;与舰队 D1 读到的「来的都是索引器」一致 |

---

## 四、把框架套到舰队现有工具上

| 形态 | ① 交付物 | ② 期限 | ③ 别人的钱包 | ④ 持续状态 | ⑤ 责任 | 否决门 | 结论 |
|---|---|---|---|---|---|---|---|
| **判定型小工具**(fanzha 12 问、codeword scam-check、firstjob 三件、learn 两件、powerbill 两件、agi timeline/test、tds fake-check) | ✗ | ✗ | ✗ | ✗ | ✗ | **踩**(ChatGPT 直接答) | **永不收费**。它们的产出是引用与信任,不是现金。继续做,但**不要再在它们身上想变现** |
| **eco 计算器**(BTU、电价、阳台光伏) | ✗ | ✗ | ✗ | ✗ | ✗ | 踩 | 它的钱在**下游的 Amazon 联盟**,不在工具本身。维持现状 |
| **SR 关税/落地成本 + 召回雷达 + Opportunity Packs** | ✓(可交付清单) | △(召回/关税变更有时效) | **✓(卖家,可报销)** | **✓(召回、税率变更)** | **✓(口径要有出处)** | 不踩(服务端数据) | **舰队里最像能收费的形态,而且已经建完了**。缺的不是形态,是需求侧读者 + owner 的 Stripe 五个值 |
| **bpj free-tier 变更告警(`watches` 钩子)+ `/api/changes`** | △ | ✗ | **✓(开发者)** | **✓(这就是 Keepa/Visualping 的形态)** | △ | 不踩(服务端时间序列) | **第二名**。同类外部产品真的在收 $14–29/月。但本站 `watches` 表 **0 行**——先有人用,再谈收费 |
| **机器面(bpj MCP + eco MCP + `/api/changes`)** | ✗ | ✗ | ✓(调用方是机器的主人) | ✓ | △ | 不踩 | 唯一有**每日回访**的面,但回访者是索引器。**先证明有非索引器的消费者**(判定线 §七) |

**一句话答 owner 的第一问**:
> 最容易被付费的工具 = **给做生意的人用、算的是钱、错了有责任、并且价值随时间累积的服务端工具**。
> 舰队里符合这个描述的只有两处:**SR 的关税/召回/Packs** 与 **bpj 的额度变更告警**。两处**都已经建好**,
> 都卡在「没人用」而不是「没想到」。所以本轮**不新建任何收费件**。

---

## 五、子站点裁定:不开

按舰队 2026-08-23 的**三条铁律**(缺一不开):

1. **独立技术形态确需隔离?** ✗ —— 收款链路(Stripe Payment Link + webhook + 交付)**SR 已经有一整套**
   (`docs/PACKS-OWNER-SETUP.md`),复用即可,没有任何需要隔离的技术形态。
2. **受众与现有站完全不同?** ✗ —— 会为工具付钱的人,就是 SR 的卖家受众和 bpj 的开发者受众,不是新受众。
3. **自带独立变现闭环?** △ —— 形式上成立(它本来就是为收费而设),但闭环的**入口是流量**,
   而新子域从零权威起步,舰队自己的读数是:六个新站上线 1–2 天,**JS 真人互动 0 次**。

再叠加舰队 09-15 自己写下的两条(`docs/new-site-pipeline.md` §四点五):
- 「**站是最贵的容器**」:一条 workflow、一套闸门、一块每日报数、一个域名。本方向能用**已有站的一页**承载。
- 「10 月四站判定线结算前,再建新站先回答:这个方向能不能用已上线站的一个新页面替代?」→ **能**。

**裁定:不开付费工具子站。** 真要做,做的位置是 **SR 的 `/packs`(已建)** 与 **bpj 的 watch 钩子(已建)**,
而且必须等 §七的判定线给出「有人用」的读数之后。

**同时要对 owner 说清楚的**:这不是「不赚钱」的建议。它是把钱押在**已经有机器每天回访、有卖家受众、
有服务端数据**的两处,而不是押在第 15 个域名上。

---

## 六、本轮做了什么(可验证,零新增 cron)

1. **六个新站补上「信标真值测试」**(SR 09-13 同款,本轮补齐 after35 / fanzha / codeword / firstjob /
   learn / powerbill):
   - 每个 worker 新增 `GET /api/selftest?label=`,只回**计数**,不回任何行级数据,且**只认 `__` 开头的自检标签**(它是 CI 探针,不是给外人读站内统计的窗口);
   - 每条 deploy 自检新增一步:先 `POST /e` 一条 `label=__ci` 的事件,再从 `/api/selftest` 读回来,
     读不到 **直接把部署打红**;
   - 六个 worker 单测各加一条断言(fanzha/codeword/firstjob/learn/powerbill 各 6 条、after35 49 条,全绿)。
   - **为什么必须有**:这六站的全部读数目前是「客户端事件 0」,而 10 月/11 月的判定线要靠
     `tool_result`/`calc_run` 结算。没有这一步,「没人用工具」与「`/e` 管道断了」在数据上完全一样。
   - **口径**:自检写的行 `label='__ci'`、`path='/__ci'`,**所有统计必须剔除**(本文 §一 SQL 已含)。
2. **把六个新站的判定线登记进 `data/fleet-bets.json`**。此前它们只写在各站 CLAUDE.md 里,
   按舰队规矩「**不进台账 = 等于没预登记**」,heartbeat 看不见,到期没人读。本轮补 12 行。
3. **两条新判定线**(§七)。

---

## 七、判定线(预登记,已进台账)

- **`fleet-tool-use-1014`(2026-10-14)**:舰队 28 天「读者主动发起的工具使用」合计(口径 = §一 SQL,
  剔 `__ci`)。**t0 = 61**。
  - **≥120** → 工具面有需求:在使用量最高的那个工具上装**一次意向件**(不是收款,是一个
    「要不要把这个结果做成可下载/可监控的东西」的事件),再按 28 天读数决定值不值得接 Stripe。
  - **<120** → 工具不是营收路径:新站三件套里「不上传的工具」**从必做降为可选**,现有工具只维护不扩建。
- **`fleet-machine-demand-1014`(2026-10-14)**:bpj `/api/*` 与 eco `/mcp` 的调用方里,**剔除本仓 CI
  (curl/8.5.0 等)、`tools/fleet/bot_ua.txt` 里的爬虫、以及自报的 MCP 采集器/索引器**(rokmcp-collector、
  SaSame-MCP-Audit、AIWebIndex…)之后,是否存在真实消费者。**t0:MCP 侧 = 0;`/api/changes` 侧有 1 个
  疑似定时调用方(SG,77 次/26 天)**。
  - **≥1 个调用方 28 天内 ≥20 次且 ≥10 个不同日期** → 机器面有真实消费者,才可以**调研**计量付费层
    (注意:**「向爬虫收费」仍然是杀单**,这条只针对主动调用 API 的开发者)。
  - 否则 → 机器面永远免费,继续按「**被 vendored**」的目标做(稳定 URL + CC BY 署名),不再提付费 API。

---

## 八、owner 侧(本轮没有新增待办,只是把旧的按「每分钟产出」重排)

1. **eco 付款/税务信息**(2 分钟)——€11,20 已经赚到,这一步是它到账的全部距离。
2. **Metaculus:`ANTHROPIC_API_KEY` + `METACULUS_BOT_ENABLED=1`**(3 分钟)——Fall 2026 主赛题 09-28 起开放。
3. **Stripe 五个值**(10 分钟)——SR Packs 的收款开关。按本文 §四,SR 是舰队最像能收费的形态;
   但**先有 ≥10 条有证据的 dossier 再开售**,这条红线不变。
4. **Reddit app 两个 Secret**(5 分钟)。

---

## 九、别再提(对外,除非本文的判定线翻转)

- 为「付费工具」开新子域/新站(三条铁律 + 「站是最贵的容器」)。
- 在**纯前端判定型工具**上装收款(view-source 即可复制,ChatGPT 一句话可替代)。
- 第五个收款面:已建四个,卖出 0 单;先把已有的两个推到「有人用」。
- 向爬虫/AI 索引器收费(舰队保险丝,09-05 已杀,本轮证据只更支持它:来的都是索引器)。
- 账号体系、订阅制会员、把不上传的工具改成要登录才能用(新站流水线硬约束:无账号、不收费)。
- 用「使用量很低所以换个更火的工具品类」来解释 61 这个数字——品类不是绑定约束,读者才是。
