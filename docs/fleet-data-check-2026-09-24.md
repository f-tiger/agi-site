# 舰队数据检查(2026-09-24)

owner 指令:「整个舰队数据检查下」。前一天 main 侧已做过一轮舰队数据梳理(`docs/fleet-review-2026-09-23.md`:pulse 与 D1 对账、
AI 探针污染六站、四站无监控)。本轮不重做那一轮,只做三件:**验证它的修复生效了**、**补它明说没做的四站来源核对**、
**扫一遍所有流水线写的数据文件是不是在更新**。读数全部来自 D1 现查、GitHub Actions 运行日志与线上请求,日期 2026-09-24。

## 一、结论先说

1. **舰队至今只有一条真实收入**:eco 的 Amazon.de 联盟(€11,20 / 30 天,窗至 09-14,付款侧未完成)。它也是唯一在涨的线:
   真人 affiliate_click 28 天 79 次,**最近 7 天 24 次,前 7 天 18 次**。其余所有收款面 0 笔。
2. **三条预登记判定线差点被我们自己的自检撑过线**。SourceRadar 的 80 次 MCP 调用里 58 次是本仓 deploy 自检;
   tds 09-23 的 304 次 pv 是一次 QA;预览主机引荐在 PR #2 的分类器里会被记成「挣到的外链」。三处都已修或已在台账记清。
3. **两条我 09-21 写下的结论被今天的数据推翻**,已在原处标注纠正(见 §四)。
4. **四个未核站(gridlings / goldrush / SR / gamesledger)的「真人 pv」大部分不是读者**,外部来源合计约 100 次/28 天。

## 二、验证 09-23 的修复

- **AI 探针污染六站:已止住**。09-23 13:10 的定时 heartbeat(旧代码)仍在六站各留 2 行「真人」;16:03 带 `ci=1` 的那次 0 行。
- **仍有两种非读者流量留在六站里**,都不是探针:①learn、fanzha 每天 10:31 UTC 各 1 次 `/`,firstjob、fanzha 另有凌晨各 1 次,
  桌面浏览器 UA、零引荐;②**powerbill 的大头是一只按站点地图逐页走的爬虫**:UA 自报 iPhone iOS 13.2.3(2019 年的版本,
  扫描器常用),约 10 分钟一页,每一页换一个国家(JP/US/SG/BR/ID/HK),从不带引荐。09-23 的判断「六站 pv 不是读者」成立。
- **heartbeat 没有坏**:当天定时运行尚未开始,是 GitHub 把 08:00 的 cron 推迟了;本周每次都在 12:10–14:43 之间启动且全部成功。

## 三、四个未核站的来源(28 天,D1 现查,真人 page_view)

| 站 | 真人 pv | 无引荐 | 其中只打根/llms/robots/sitemap | 站内跳转 | 搜索 | 兄弟站 | AI |
|---|---|---|---|---|---|---|---|
| gridlings | 797 | 549 | — | 208 | 27 | 8 | 0 |
| goldrush | 452 | 423 | 285 | 6 | 14 | 8 | 0 |
| SourceRadar | 106 | 99 | 78 | 0 | 6(同一天) | 0 | 0 |
| gamesledger | 513 | 400 | 196 | 80 | 25 | 6 | 1 |

- **gridlings 自有域的「游玩」基本是开发与测试**:123 次 solve 里 122 次落在 6 天(09-01→09-08、09-17),国家只有 CN 与 US;
  US 也是会话与 CI 的出口。外部玩家只从 Playgama(185 次开局)与 itch(37 次)来。
- **gamesledger 的 80 次站内跳转来自 32 个国家、18 天**:80 次访问摊到 32 国不像读者,更像带 referer 的轮换代理爬虫,记为未证实。
- 这四站外部来源(搜索 + 兄弟站 + AI + 游戏门户页面引荐)合计约 **100 次 / 28 天**。

## 四、我自己 09-21 的两条错误结论(已在原处标注)

1. 「SR `/api/mcp` 09-17 起 24 次非 CI 调用,带参数的 check_import_claim 7 次 = 机器面首次真实使用」—— **错**。
   worker 把调用方 UA 存在 `ref` 列,按它拆:**45 次是 deploy 自检 UA,13 次是同一步骤里没带 `-A` 的裸 curl**,合计 58/80;
   外部 22 次来自 8 个调用方,全部是索引器,**带参数的外部调用 0**。那 7 次 check_import_claim 就是 deploy 的 JSON-RPC。
2. 「bpj 广告位建了没通电,缺两个密钥」—— **错**。09-22 部署日志里 `/api/ads?doctor=1` 回 `selling:true`、钱包轨(BSC USDT)已通电,
   缺的只是卡轨。它属于「通了没人买」:ads / 订单 / 链上收据全部 0 行。

## 五、本轮修掉的东西

1. **SR deploy 自检的 JSON-RPC 两发补上 `-A "$UA"`**,以后每一次自检调用都能按 UA 剔除。台账三条线(`sr-mcp-calls-1014`、
   `sr-vertical-1014`、`fleet-machine-demand-1014`)写入今天的剔除后读数:22 次 / 0 个合格调用方 / 带参数外部调用 0;
   `fleet-machine-demand-1014` 加一条不改阈值的澄清:本仓自检永远不算调用方(否则下一个部署日就会自己满足「跨 ≥5 天」)。
2. **PR #2 的来源分类器把 `*.pages.dev` / `*.workers.dev` 归为 self**。不改的话,tds 09-23 在预览主机上的那次 QA 会在
   `by_other` 里记 71 次「挣到的外链」,单独就过 `fleet-backlinks-1116` 的 ≥20 次阈值。13 个 worker 经 `--sync` 同步,
   checker 两个方向各加用例(`dollscout.pages.dev`→self,`pages.dev.evil.example`→other)。
3. **台账补读数**:`tds-60d-1029`(09-23 那 304 次 pv 须按天剔除)、`bpj-ad-inventory-1014`(收银台已开、需求为 0)、
   `gridlings-itch-0924`(58 与 52 对不上的原因:D1 无删行,58 是离群读数,结算值 52 成立)。

## 六、数据文件新鲜度

在 main 上扫了 85 个带生成日期的数据文件。超过 3 天未更新的多是参考数据或按周轮转的 Trends 抓取,属正常。
**唯一的真问题是 bpj**:变更日志 `limits-history.json`、行动队列 `agenda.json`、`mcp-status.json`、`ai-crawler-probe.json`
自 09-11 起没有进过仓。根因在 09-22 的运行日志里:两个提交步骤用 `git pull --rebase` 而不带 `--autostash`,
报「cannot pull with rebase: You have unstaged changes」退出 128,又被 `continue-on-error` 染成绿色。
**main 今天上午的 `5a24095` 已给两处都加了 `--autostash`**,判定线 `bpj-manifest-commit-0927` 已在盯,本轮不重复改。
同一份日志里 Wayback 存档 0/8 报 500/429,核实是 archive.org 的 save 接口拒绝,页面本身 200。

## 七、没做的

- 不改任何站的 pulse 定义与计数口径;不删任何数据行。
- bpj 8 条厂商投稿至今全部停在 `new`,最早一条 09-07,没有一条被审过。审核要逐条核实免费额度,是内容工作,另起任务。
- 不结算未到期的线。明天到期的 eco 五条与已结的 Playgama 五款由各自会话按原文结算。
