# 舰队互相学习 · 逐条矩阵(2026-09-15)

owner:「我不是让你详细相互学习?」——第一轮我只查了「文件在不在」(sitemap/IndexNow/llms.txt),
那是清单不是学习。这一轮改成**量线上页面**:同一套判据打在 14 个站上,谁强谁弱一张表看完。

## 一、矩阵(分层抽样;分母是抽到的页数,不是全站)

| 站 | 页数 | 抽样 | 带日期新鲜度 | 有一手源 | FAQ-LD | 面包屑 |
|---|---:|---:|---:|---:|---:|---:|
| baipiaoji | 1558 | 16 | **12/16** | 7/16 | 10/16 | **16/16** |
| learn / fanzha / firstjob | 8–9 | 2 | 2/2 | 1/2 | 1/2 | 1/2 |
| thedollscout | 39 | 11 | 4/11 | 6/11 | 4/11 | 5/11 |
| **agiscorecard** | 212 | 16 | **4/16** | **16/16** | **16/16** | **3/16** |
| getecoback | 206 | 9 | **2/9** | **9/9** | 6/9 | 6/9 |
| gridlings | 45 | 4 | 0/4 | 2/4 | 4/4 | 0/4 |
| buysomething | 5 | 2 | 0/2 | 1/2 | 1/2 | 0/2 |
| goldrush / gamesledger / after35 / codeword / powerbill | 5–20 | 2 | 1/2 | 0–2/2 | 0–1/2 | 0–1/2 |

仪器:`tools/fleet/page_patterns.py`(每周一随 heartbeat 取数,零新 cron)→ `data/fleet-page-patterns.json`。
小站只有 1 个一级路径,所以只抽到 2 页 —— **它们的行是弱证据,别据此下结论。**

## 二、逐条:谁教谁

### 1. 带日期的新鲜度标注 —— **bpj → agi / eco**(方向与直觉相反)

- **bpj 12/16(75%)是全舰队最好的**,而且是在 1558 页的规模上做到的(「核实于 2026-08-02」逐条挂在福利条目上)。
- **agi 只有 4/16。** 这是它自己站规里写死的东西:GEO 研究「带日期的统计 +37%」、判定页六件套第 ⑤ 条
  「带日期的一手判定」。**规则写了,页面上没做到。**
- eco 2/9 更低。
- **这一条最值得移植,而且移植方向是从「小弟」到「旗舰」。** 但 agi 有防翻炒规则(近 5 次改过的页不动)
  与生成器漂移风险(站规明写「跑任何 generator 之前先读那一节」),**批量改 212 页正是它警告过的操作**——
  所以本轮只出读数与判定线,实际改动留给在 agi 目录内、按 OPT-LOG 纪律работа的那次会话。

### 2. 一手源外链 —— **agi / eco → bpj / tds / after35**

- **agi 16/16、eco 9/9,每一页都至少挂一条站外一手源。** 这是 GEO 研究里排第一的技术(+40%)。
- bpj 7/16、tds 6/11。bpj 的 `/tools/` 页每页都有厂商官网链接(带 `?utm_source=baipiaoji` 与 nofollow),
  但 `/vs/` 对比页、`/wall/` 等大类没有。
- **这一条不急着动**:bpj 的 219 个工具页已经做对了,缺的是对比页,而对比页的事实来自两个工具页的数据,
  加链接要判断加谁的——属内容决定,不是机械补洞。

### 3. 面包屑 BreadcrumbList —— **bpj → agi**

- bpj **16/16**,agi **3/16**,而 agi 站规写着「每个新页:BreadcrumbList JSON-LD(Home → AGI questions → page)」。
- gridlings / goldrush / after35 / gamesledger / buysomething 为 0 —— 其中游戏与工具站本来就不吃引用(站规已定论),优先级低。

### 4. FAQPage JSON-LD —— 基本齐了

agi 16/16、bpj 10/16、eco 6/9、gridlings 4/4。after35 与 gamesledger 抽到的 2 页为 0,但样本太薄,不下结论。

## 三、本轮真正修掉的东西:bpj 的 1558 条 sitemap URL 全是跳板

**这是本轮唯一一个「只有一个站做错、另外 13 个站都做对」的硬缺陷,而且它落在页数最多、
且唯一有真实 Google 引荐(157/28d)的站上。**

- Cloudflare Pages 把 `/x.html` **308** 跳到 `/x`。bpj 的 sitemap、`canonical`、`og:url`、`hreflang`、
  `llms.txt` 全部声明 `.html` 版本 —— **即 canonical 指向一个非 200 的地址**,sitemap 每条多一跳,
  IndexNow 每次推的也是跳转地址。
- 抽样实测:bpj 6/6 重定向,**另外 13 个站 0/6**。
- 改法只动**对外声明**的四处(`pub()` 与 `pubText()`),站内 href 与构建期路径一律不动 ——
  后者是 `.md` 镜像与 dist 落盘的键,动了收益小、面大。
- 本地构建验证:1558 条 loc **零 `.html`**;全站 canonical/og:url 里剩下的唯一一条 `.html` 是
  `/404.html`(有意保留);hreflang 零;`llms.txt` 的 URL 已剥离而「`.html` 换 `.md`」的镜像说明完好;
  `/vs/` 这类目录页不受影响。
- **部署时会发生一件必须预先讲清楚的事**:canonical 与 hreflang 在每一页上,所以构建会判定
  **1546/1558 页内容有变**,lastmod 全部刷新为部署日,IndexNow 随之整站推一次。这是一次性的,
  而且是诚实的——这些页面的抓取语义确实变了。但别把它误读成「又一次全站重推」的老毛病。
- 守卫:`tools/fleet/sitemap_guard.py` 挂 heartbeat(14 站各抽 4 条,3xx 即红),
  bpj 部署自检另加一条(sitemap 零 `.html` + 抽样页直接 200 + canonical 自指)。

## 四、两个我自己犯的测量错误(比结论更值得记)

舰队站规有一条:「一个洗字段的函数出错时不会报错,只会让表看起来很正常。」这一轮我连踩两次,
两次都已写成 `page_patterns.py` 的自检用例:

1. **只抽 sitemap 开头 = 把整站当成一种页面。** bpj 的 sitemap 前 16 条全是 `/vs/` 对比页,
   据此我一度得出「bpj 全站零一手源」——错的,它的 `/tools/` 页每页都有。改成**按一级路径分层抽样**。
2. **判外链前必须先剥 query。** bpj 的出站链接都带 `?utm_source=baipiaoji`,我按整串找站名,
   于是**每一条外链都被当成了站内链接**,整站外链数归零。
3. (第三个,同类)**新鲜度词表漏了「核实于」**,于是把全舰队做得最好的站(bpj 12/16)误判成 0/7。
   **各站措辞不同,词表漏一个就会把最好的站读成最差的。**

**通用教训**:跨站比较最容易死在「我的尺子对 A 站有效、对 B 站无效」上,而它不会报错,只会给你一张
看起来很整齐的表。凡是跨站矩阵,每条判据都要先在「已知有」和「已知没有」的两个站上各验一次。

## 五、判定线(已进 `data/fleet-bets.json`)

- **`bpj-canonical-fix-1013`**:10-13 前 bpj 的 `google.*` 引荐 ≥180/28d(09-15 基线 157)→ 卫生有效;
  否则记反面发现「canonical 卫生不是 bpj 的瓶颈」,守卫保留但不再据此改站。
- **`fleet-xlearn-matrix-1013`**:agi 或 eco 的 `dated_fresh` 命中率 ≥50%(基线 agi 4/16、eco 2/9)
  → 矩阵能真的驱动移植;否则降为季度一次。

## 六、本轮**不做**的(附理由,别再提)

- **不批量改 agi 的 212 页**去补新鲜度行与面包屑:防翻炒 + 生成器落后于页面(站规明写的事故形状)。
  该由在 agi 目录内、跑 `git diff` 逐个确认的那次会话做。
- **不给 bpj 的对比页机械补外链**:加谁的链接是内容判断,不是补洞。
- **不据小站 2 页的样本下任何结论**:它们只有一个一级路径,样本天然薄。
