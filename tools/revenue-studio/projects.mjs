export const projects=[
{id:'orders',name:'Refund-aware revenue',brief:'List each customer with net paid revenue. Include customers with no paid orders. Deduct refunds and exclude cancelled orders. Order by net_revenue descending, then name.',schema:'customers(id, name) · orders(id, customer_id, status, gross, refund)',setup:`CREATE TABLE customers(id INTEGER PRIMARY KEY,name TEXT);CREATE TABLE orders(id INTEGER PRIMARY KEY,customer_id INTEGER,status TEXT,gross REAL,refund REAL);INSERT INTO customers VALUES(1,'Aster'),(2,'Birch'),(3,'Cedar'),(4,'Dune');INSERT INTO orders VALUES(1,1,'paid',100,20),(2,1,'cancelled',200,0),(3,2,'paid',60,0),(4,2,'paid',40,40),(5,4,'paid',30,0);`,starter:'SELECT * FROM orders;',answer:`SELECT c.name, COALESCE(SUM(CASE WHEN o.status='paid' THEN o.gross-o.refund ELSE 0 END),0) AS net_revenue FROM customers c LEFT JOIN orders o ON o.customer_id=c.id GROUP BY c.id,c.name ORDER BY net_revenue DESC,c.name;`,explanation:'LEFT JOIN preserves customers without orders. The CASE lives inside SUM so cancelled orders do not remove their customer. Refunds reduce paid revenue.'},
{id:'inventory',name:'A reliable reorder queue',brief:'Find items whose stock across all locations is below reorder_point. Include items without stock rows as zero. Output sku, available, reorder_point, ordered by sku.',schema:'items(sku, reorder_point) · stock(sku, location, units)',setup:`CREATE TABLE items(sku TEXT PRIMARY KEY,reorder_point INTEGER);CREATE TABLE stock(sku TEXT,location TEXT,units INTEGER);INSERT INTO items VALUES('A-10',12),('B-20',5),('C-30',8),('D-40',3);INSERT INTO stock VALUES('A-10','East',4),('A-10','West',5),('B-20','East',7),('C-30','West',8);`,starter:'SELECT * FROM items;',answer:`SELECT i.sku,COALESCE(SUM(s.units),0) AS available,i.reorder_point FROM items i LEFT JOIN stock s ON s.sku=i.sku GROUP BY i.sku,i.reorder_point HAVING COALESCE(SUM(s.units),0)<i.reorder_point ORDER BY i.sku;`,explanation:'Aggregate locations before testing the reorder threshold. HAVING filters groups; COALESCE treats no stock rows as zero. Stock equal to the threshold is not below it.'},
{id:'billing',name:'Duplicate invoice exceptions',brief:'Find vendor/invoice pairs recorded more than once. Output vendor, invoice, copies, amount_total, ordered by vendor and invoice. Different vendors can use the same invoice number.',schema:'invoices(vendor, invoice, amount)',setup:`CREATE TABLE invoices(vendor TEXT,invoice TEXT,amount REAL);INSERT INTO invoices VALUES('Alto','001',100),('Alto','001',100),('Alto','002',50),('Boreal','001',80),('Boreal','003',30),('Boreal','003',30),('Boreal','003',30);`,starter:'SELECT * FROM invoices;',answer:`SELECT vendor,invoice,COUNT(*) AS copies,SUM(amount) AS amount_total FROM invoices GROUP BY vendor,invoice HAVING COUNT(*)>1 ORDER BY vendor,invoice;`,explanation:'The duplicate key includes vendor and invoice. Grouping only by invoice would incorrectly merge distinct suppliers. Exceptions still need a human review; this does not authorize payments.'}
];
