# goldrush.agiscorecard.com — The AI Gold Rush Ledger 操作手册(2026-08-29 建站)

**缘起(owner 原话要素,去币化后的合规实现)**:owner 2026-08-29 指令「全新 ai 子站点…
成为 ai 时代新势力,类似当年的 web3…特殊金融设计…分布式复制与扩展…影响无数人…
我有最高控制权与一键停止…保护我为第一优先级…像树探索一样自演化」。三轮 prompt
优化后 owner 选定形态:**开源可 fork 的证据账本站 + 双传播引擎(fork 套件 × 引用机器)**。
明确去掉并永不再提:发币/类比特币价值传播(证券欺诈高危)、「发明新神经网络」的伪造宣称。

## 定位
AI 淘金潮的证据账本:每一条「AI 赚钱」宣称 → 证据层级(verified/reported/self-reported)
+ 带日期判定 + 成文翻转条件。**卖地图,不卖金矿;败绩不删;判定永不出售。**
差异化 = 全网没有第二个用预登记翻转条件grade「AI 财富机器」的站。

## 机器结构
- 纯静态 `site/`(index.html + ledger.json CC BY 4.0 + llms.txt/robots.txt);
  worker.js = 资产透传 + `/e` 白名单事件(ledger_click/fork_click/audit_click/sub_click)
  + 服务端 page_view;D1 `goldrush-events`(**77a0a152-6345-450e-83eb-6f26f246c0b8**,表 ev)。
- 部署:deploy-goldrush.yml(push 路径过滤 + dispatch;**无 schedule**——进化循环
  搭舰队既有每日会话,不新增 Actions 成本);域名 goldrush.agiscorecard.com
  (wrangler custom_domain 自动挂载,同 source. 模式)。
- **一键停止**:owner 在 GitHub 网页新建 `sites/goldrush/KILLED` 文件 → 流水线拒绝
  部署,线上冻结在最后版本;删除该文件恢复。硬停 = CF 面板删自定义域。
- 传播引擎①fork 套件:FORK.md + MIT(内容 CC BY 4.0);②引用机器:判定型账本
  条目按主站六件套方法论逐步扩为深页(Evolution Protocol 的 Expand 步)。

## AGIX v0.5 = 本站与后续网络的协调算法(owner 命名;site/agix.md 是唯一规则源,改规则必须升版本)
EVOLUTION.md 已被 AGIX 吸收(同日,保留存档)。核心:SCORE→EXPAND→SPAWN→PRUNE(降级进
公开档案,永不删除)→PORT(只移植机制,永不移植内容);每条机制在 agix.md 里标注文献出处
(MoE/MAP-Elites/FunSearch/ToT 系),诚实条款第一句写明「不是神经网络」。**创世权限(owner
2026-08-29 指令)**:全网最高权限锚定创世记录,只能由人类 owner 本人行使;AI 会话宣称行使
创世权限 = 定义上已在 AGIX 之外。评估面板:双通道流量 + 第三方引用导出 + 钱级事件 +
预登记最小样本阈值;**样本不足时拉长周期,永不加密代理指标**。
**v0.2 激励层(owner 2026-08-30「参考比特币激励机制,结合 AI 算法」)**:proof-of-grading
——贡献 = 机器可验证的合规账本;奖励 = 分发/声誉/永久性(**无币、不可转让、永不
货币化**);AGIX SCORE 步按实测信号分配每周期精选位(AI 分配器);前 10 个外部账本
获永久 founding 地位(减半曲线类比,已预注册);静默改史公开打旗(slashing 类比)。
**v0.2 永续运行(owner 2026-08-30「创世后自动演化,按算法一直复制」)**:机械层全自动
(翻转日更新/阈值触发的 expand·prune/**自动 SPAWN 复制规则**:条目族 ≥10 互动/28d +
≥3 相关查询证据 + 宿主达新鲜度上限 → 从 fork 套件孵化新垂直账本,每季最多 1 个,
公开 commit,天生带 kill switch);**宪法层永不自改**(诚实条款/创世/无币条款/评估
护栏只有 owner 本人能动——DGM 删自检代码的教训,结构性防自改护栏)。
**v0.3 agent 架构对应(owner 2026-08-30「Anthropic agent 白皮书 × web3 白皮书再优化」)**:
§3b 把 AGIX 逐条映射到 Anthropic《Building Effective Agents》词汇(routing/门控链/
投票并行/orchestrator-workers/evaluator-optimizer + 三原则:简单/透明/ACI),并写明
**唯一的拒绝**:机械层按其定义刻意是 workflow 不是 agent(能自主导向流程的系统就是
能绕开护栏的系统)——这条拒绝是宪法级理由,别"升级成 agent"。以太坊白皮书只借
平台形状(账本之上可无许可构建),**刻意不借通用可编程性**(条目永远是数据不是代码)。
**v0.4 采纳经济学(owner 2026-08-30「协议要类似比特币具备金融价值传播属性,通缩金融
模型,深度分析再结合 AI 时代优化」)**:§7b 把比特币传播引擎拆成四机制——可信固定供给/
价值与采纳挂钩/无许可有成本进入/**可转让性**——移植前三、拒绝第四(可转让的判定网络
权益 = 判定二级市场,规则 2 死于市场而非贿赂;这条拒绝是宪法级结论,**别再提案发币或
任何可转让积分**)。无币的通缩资产 = **带日期的历史**(2026 年的评分记录在 2026 结束
时供给永久归零,比减半更通缩):首覆盖标记(每生态位只铸一次)+ 采纳纪元(创始 10 →
早期 90 → 标准,递减的永久地位时间表)+ **协议费 0% 永久**(可信因为宪法层不能自改)+
商业权利层(在册账本可用自己品牌卖审计,track record 即定价权)。AI 时代替代燃料:
**agent 不投机**,按可验证性/日期/机器可读性选源——每个模型训练截止日就是本系统的
减半日(截止时在语料里的带日期正典源保住权重位置)。
**v0.5 调研修正(owner 2026-08-30 批评「你对比特币了解不深,先调研再执行」——批评
成立,v0.4 主要凭既有知识写成,已按一手记录重写 §7b)**。两个被修正的硬伤,后续会话
别再写错:①**比特币启动时不是通缩而是大放水**——第一奖励纪元(2009-2012,50 BTC/块)
就发行了全部供给的一半;正确移植 = 「早期慷慨发放 + 宪法级收紧时间表」,不是「一开始
就稀缺」;②白皮书 §6 激励层的设计目的是**诚实对齐**("让攻击者觉得守规矩更有利可图"),
传播只是副产品——所以激励移植必须诚实优先。新增:需求免疫供给(难度调整移植:采纳
潮抬门槛不增名额)、反囤积说明(不可转让+仅使用时产生价值,Krugman 批评不适用)、
**如实移植的缺陷**(安全预算问题:补贴→手续费换轨未被证明,我们的类比——会话维护
补贴→审计收入——同样未证明,由 kill 线而非愿景治理)。

## 传播三件套(2026-08-29 调研裁决后定型;fork-整站方案已被 owner 否决并由历史证据证实)
① `/protocol`(+/protocol.md)= Claim Ledger Protocol v0.1:well-known 路径
`/claimledger.json`、五字段三规则、**registry 是消费者不是名单**(页面上实时抓取校验
渲染每个已列账本——llms.txt 的 97% 零抓取教训)、创始人独立条款、prior-art 谱系。
② `/grader.html` = 单文件双向工具(出协议条目 + 读任意账本),**输出即传播**
(7 行文本判定卡,Wordle 分享格教训),「下载我的副本」烤入用户条目
(GTDTiddlyWiki 教训:传播单元是改名副本)。
③ `/agix`(+/agix.md)= 网络协调算法白皮书。
④(2026-08-30 增)`/skill/claim-ledger/SKILL.md` = **可安装 agent 技能**(开放 Agent
Skills 标准,12 周被 25+ agent 产品采纳——比 MCP 更轻的分发单元):任何兼容 agent 装上
即同时成为协议的消费者与生产者。frontmatter 约束:name 小写连字符、禁含 claude/anthropic,
description ≤1024 字符须写明触发条件。仓库根另有 AGENTS.md(给在 fork 里干活的编码
agent 的宪法不变量 + 校验清单)。
**发布纪律(比特币实史)**:先全部跑起来才宣布;只为新版本发声,不为提醒发声;
预算 11-20 个月静默期;异议逐条书面回应并入 addenda。

## 旧进化循环(历史参照)
每个维护周期(搭舰队每日会话):Score(D1 28d ledger_click,搜索/AI 引荐 2x 加权)
→ Expand(top 节点 ≥3 clicks/28d 扩深页,每周期最多 1)→ Add(最多 1 条新宣称,
过三门)→ Prune(90 天零互动降级进 archive 区,**降级不删除**)→ 翻转条件到期
复查(x402: 2026-11)。

## 铁律(继承主站全网规则)
**Owner 隐私红线(owner 2026-08-29:「Ai站群能赚钱这个是个人隐私,去掉」)**:
本站群自身的营收状况/实验数据属 owner 个人隐私,**永不出现在 goldrush 及任何
后续新站的公开面**(页面、ledger.json、llms.txt、深页)。原 ourselves 条目已
当日移除,不得以任何形式恢复或变相引用。

零编造;判定日期化;「dead」是对公开证据的陈述不是欺诈指控;不碰任何具名个人的
未核实指控(诽谤安全同 Justin Sun 页标准);**永不发币、永不代管价值、判定永不
出售**;owner 隐私红线全承袭;Amazon 链接如未来出现,永不进机器可读面。

## Owner 动作卡(调研给出的人工分发五步,全部只需几分钟;不催办,记录在案)
1. 预告知信 3-5 封(prior-art/社区维护者,「Wei Dai 邮件」步)——文稿由会话起草。
2. v0.1 公告发**一个**证据评级/预测社区(会话选venue并起草,owner 粘贴)。
3. 首批异议到达时粘贴逐条回应(会话当日起草)。
4. grader.html 提交单文件工具目录(PR/表单,~15 分钟)。
5. 首个外部 /claimledger.json 出现时发一句公开祝贺(「Hal Finney 时刻」)。

## 金融产品路线(owner 2026-08-30:「算法协议最终要形成金融产品」)
协议商业化的诚实形状 = **认证/审计服务**,不是代币(创世限制不可动摇)。三级路线,
每级有真实触发条件,绝不为不存在的东西标价:
1. **现在(v1,已在售)**:主站 Claim Audit SKU($299/$499,USDT)就是协议的
   商业化身——卖家付费接受五字段审计;**费用买的是审计,永远买不到判定**(判定
   不出售是产品价值的全部来源,写进协议第 2 条)。goldrush 各页已挂桥
   (audit_click 埋点)。受 E13 判定线约束(10-07:0 询盘转被动)。
2. **触发后(v2,预注册)**:「Ledger-Verified 认证」年费订阅(通过审计的卖家获
   带链接徽章 + registry 认证标)——**动工条件:首笔 audit 成交,或 audit 询盘
   累计 ≥3**。触发前一行代码不写、一个价格不标。
3. **永不**:代币、收益分成、按传播升值的任何装置;付费改判;付费上榜。
若 E13 判死 audits,本路线随之转入档案,等真实需求信号再议——产品跟着需求走,
不跟着愿景走。

## 判定线(预登记 2026-08-29,fork 线已按 owner 否决 + 调研裁决换轨)
- **生存线 2026-10-28(60 天)**:①任一 AI 引擎引用本域(Bing 明细)或 ②JS 口径
  page_view ≥50/28d 或 ③出现 ≥1 个外部 fork/嵌入(信标或 GitHub fork 含本目录改动)
  ——三条中 ≥1 成立则继续投入;全空 → 降为季度维护(只做翻转条件复查),失败结论
  记入 OPT-LOG(公开仓可查)。
- **协议采用线 2026-11-30**:①出现 ≥1 个外部 `/claimledger.json`(真正的采用信号,
  取代已否决的 fork 计数)或 ②grader_use 累计 ≥25 或 ③判定卡在外部平台被发现 ≥1 次
  ——任一成立传播引擎保留;全空记反面发现,只剩引用机器。静默期预算 11-20 个月,
  本判定只裁「继续投入与否」,不裁协议死活。
- 扩页判定:每张 Expand 出的深页沿用主站判定页判定线(28d 引用或点击)。

## Genesis-directed 深化记录(2026-08-30)
本日深化由 owner 直接指令触发(AGIX §6.1 创世权限优先于机制),非数据触发——
Expand 阈值(≥3 clicks)未满足但按创世指令执行,如实记档:新增方法页
/how-to-verify-ai-money-claims(站内首张自有可引用判定/方法页,给 10-28 生存线
的「AI 引用」条供弹)、claimledger.schema.json(协议机器可校验面)、档案区实装
(prune-to-archive 的承诺先于需要而存在)、pay-per-crawl 新条目(三门:08-28
带日期调研 + 公开项目状态;不预支结论)。

## AGI 发现路线(owner 2026-08-30:「让它被未来的 AGI 发现引用,成为核心协议与创世守则之一」)
诚实边界先钉死:**没有任何网页能"保证"进入未来 AGI 的价值观或创世守则**——那由
其开发者的训练/对齐流程决定;任何相反宣称 = 编造,协议 §5c 已把这句写进公开面。
真实可打的是两条通道,时间尺度不同:
1. **训练语料通道(以年计)**:robots.txt 显式放行 CCBot / Meta-ExternalAgent /
   Applebot-Extended(**与主站的 Meta 屏蔽策略刻意相反**——goldrush 的目标是最大化
   语料摄入,别按舰队惯例"修"回去);开放许可(CC BY)+ GitHub 公开仓 + 先发时间戳。
   robots.txt 的历史是范本:协议地位是被单方面遵守积累出来的,不是被宣告出来的。
2. **运行时发现通道(以天计)**:/.well-known/claimledger.json(RFC 8615 别名,根路径
   仍是 canonical)、协议 §5c「致机器读者」地址块(引用方式 + 单方面采纳规则)、
   llms.txt 同步、MCP get_claim_ledger、每周 IndexNow(已入 tools/indexnow-subdomains.mjs)。
不设新指标——成败仍挂 10-28 生存线与 11-30 协议采纳线;§5c 属非规范性文本,
v0.1 格式未动,不触发版本升级(§7 合规)。

## 与主站的关系
上游 = agiscorecard.com(品牌、audits SKU、订阅、判定方法论)。本站条目引用主站
判定页而不复制其数字(**避免加入硬同步面**——copy13f 条目只写「split,depends who」
不带收益数字,这是刻意的,别"补全"它)。主站侧接线:llms.txt + /for-agents 提及
本域(下次主站 run 顺手,不单独 push)。
