# Project skills (curated)

Curated agent skills matching this project's workflow (static-site SEO/GEO
growth engine). Each was read in full and security-reviewed before vendoring
(instruction text only — no commands, no network calls). MIT-licensed sources.

## From ECC (github.com/affaan-m/ECC), added 2026-07-11
- `seo/` — technical/on-page audit checklists; use in daily OPTIMIZE mode.
- `article-writing/` — long-form quality bar + banned-AI-patterns list; use
  when writing or refreshing GEO pages.
- `growth-log/` — pattern-extraction methodology; use when writing OPT-LOG
  entries and "Facts that gate automation" learnings (title the pattern, not
  the event; every entry needs a "next time I will..." + a recognition signal).

## From marketingskills (github.com/coreyhaines31/marketingskills), added 2026-07-12
Repo-wide security scan clean (only curl hits were documented vendor-API
examples). Selected the 4 that match a static-site growth engine; skipped the
other 43 (email/sms/ads/social/video — not our channel).
- `ai-seo/` — answer-engine optimization (GEO/AEO/LLMO). Carries the Princeton
  GEO ranking: **cite sources +40%, statistics +37%, quotations +30%**;
  single-H1 = 2.8× citation rate. The playbook for the AI Assistant channel
  (our KPI proxy). Use when refreshing pages or planning citation strategy.
- `directory-submissions/` — backlink/DR channel: high-DR directory listings
  raise DR + get pulled into AI answers (AI-referred traffic converts 6–27×).
  Basis for backlink-kit.md. Honest-fit rule: we're a free data resource, not
  a SaaS — skip SaaS-only directories.
- `programmatic-seo/` — pSEO at scale (our comparison/zh-parity batches are
  this). Reference when seeding a new page batch.
- `cro/` — conversion-rate optimization; use on subscribe CTAs (subscribe_click
  is the revenue KPI, currently low).

## Curation rule
Do NOT bulk-import skill catalogs — most of any catalog is irrelevant here and
would pollute agent context. Review any addition in full + security-scan first.
Match strictly to the growth workflow (SEO/GEO/content/analytics/CRO/backlinks).
