export const schema=`CREATE TABLE orders (id INTEGER PRIMARY KEY, customer TEXT, country TEXT, status TEXT, revenue REAL, refund REAL);`;
export const datasets=[[[1,'Ada','DE','paid',120,0],[2,'Bo','FR','paid',80,20],[3,'Ada','DE','cancelled',50,0],[4,'Cy','DE','paid',90,90],[5,'Di','FR','paid',200,0],[6,'Bo','FR','paid',40,0]],[[1,'Ada','DE','paid',150,10],[2,'Bo','FR','cancelled',80,0],[3,'Cy','GB','paid',60,0],[4,'Ada','DE','paid',30,30],[5,'Di','FR','paid',100,20],[6,'Di','FR','paid',25,0]]];
export const challenges=[
{title:'01 / Find paid orders',brief:'Return id and customer for paid orders, ordered by id. Cancelled orders do not count.',columns:['id','customer'],hint:"Use WHERE status = 'paid' and ORDER BY id.",solution:"SELECT id, customer FROM orders WHERE status = 'paid' ORDER BY id;"},
{title:'02 / Measure net revenue',brief:'Return one column named net_revenue: the sum of revenue minus refund for paid orders. Include fully refunded paid orders as zero.',columns:['net_revenue'],hint:'Filter paid rows, then SUM(revenue - refund). Alias the result AS net_revenue.',solution:"SELECT SUM(revenue - refund) AS net_revenue FROM orders WHERE status = 'paid';"},
{title:'03 / Write the country report',brief:'Return country and net_revenue for paid orders, grouped by country, ordered by net_revenue descending and country ascending for ties.',columns:['country','net_revenue'],hint:'Combine WHERE, GROUP BY, SUM and ORDER BY. Do not subtract refunds twice.',solution:"SELECT country, SUM(revenue - refund) AS net_revenue FROM orders WHERE status = 'paid' GROUP BY country ORDER BY net_revenue DESC, country ASC;"}
];
export function seed(db,rows){db.run(schema);const s=db.prepare('INSERT INTO orders VALUES (?,?,?,?,?,?)');for(const r of rows)s.run(r);s.free();db.run('PRAGMA query_only=ON');}
export function validateQuery(sql){if(typeof sql!=='string'||sql.length>6000||!/^\s*(SELECT|WITH)\b/i.test(sql))throw Error('Use a SELECT or WITH query of up to 6,000 characters.');const clean=sql.trim().replace(/;$/,'');if(clean.includes(';')||/\b(?:randomblob|zeroblob|load_extension|attach|pragma)\b/i.test(clean))throw Error('Use one read-only query without blob or extension functions.');return clean;}
export function equalResults(a,b){return JSON.stringify(a)===JSON.stringify(b);}
