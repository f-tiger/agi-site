export const account=process.env.CLOUDFLARE_ACCOUNT_ID;
export async function api(path,{method='GET',payload,zone=false}={}){const token=zone?(process.env.CLOUDFLARE_API_TOKEN_ZONE||process.env.CLOUDFLARE_API_TOKEN):process.env.CLOUDFLARE_API_TOKEN;if(!account||!token)throw Error('Cloudflare credentials are unavailable.');const response=await fetch('https://api.cloudflare.com/client/v4/'+path,{method,headers:{Authorization:'Bearer '+token,'Content-Type':'application/json'},...(payload?{body:JSON.stringify(payload)}:{}),signal:AbortSignal.timeout(20000)});const data=await response.json();if(!response.ok||!data.success)throw Error('Cloudflare '+(zone?'zone':'account')+' operation failed: HTTP '+response.status);return data.result;}
export async function database(){const list=await api(`accounts/${account}/d1/database?name=agi-venture-lab&per_page=100`);const matches=list.filter(x=>x.name==='agi-venture-lab');if(matches.length>1)throw Error('Ambiguous experiment database.');return matches[0];}
