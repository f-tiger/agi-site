export function pilotEntry(BASE,zh,source){return `<p class="quote-notice"><a href="${BASE}/studio/release-check?source=${source}"><b>${zh?'上线收费应用前，先梳理付款与权限验收':'Launching a paid app? Plan the payment and access checks'}</b></a><br>${zh?'免费检查清单与报告示例；可申请拟议 $299 的单应用验收试点。':'Free checklist and example report; request a proposed $299 single-app review.'}</p>`;}
export function buildReleasePilot({layout,railOf,crumbLd,faqLd,BASE,NAME,LOCALE,site,write,pushPage}){
 const zh=LOCALE.code==='zh',t=(z,e)=>zh?z:e,path='/studio/release-check',asset=site.base_url+'/studio-assets';
 const title=t('付款成功，权限也正确吗？','Payment worked. Did access change correctly?');
 const desc=t('为维护收费应用的开发工作室准备：梳理订阅、账户与权限检查，查看示例，再决定是否申请单应用验收试点。','For studios maintaining paid apps: map subscription, account and access checks, inspect an example, then decide whether to request a single-app review.');
 const fields=[['role',t('你的采购角色','Your purchasing role'),[['owner',t('我可批准这笔服务采购','I can approve this service purchase')],['developer',t('开发或交付负责人，需他人批准预算','Developer or delivery lead; budget needs approval')],['other',t('其他','Other')]]],['task',t('下一项真实任务','Your next real task'),[['launch',t('新版本上线','Release a new version')],['plans',t('修改付费套餐或权益','Change paid plans or access rules')],['auth',t('认证或账户变更','Change authentication or accounts')],['other',t('还没有具体任务','No specific task yet')]]],['frequency',t('目前维护几个收费应用','Paid apps you currently maintain'),[['zero','0'],['one','1'],['two-plus',t('2 个及以上','2 or more')]]],['timing',t('任务时间','When it is needed'),[['14days',t('未来 14 天','Within 14 days')],['30days',t('未来 30 天','Within 30 days')],['later',t('更晚或未定','Later or undecided')]]],['stack',t('支付与认证组合','Payment and authentication setup'),[['stripe-external',t('Stripe 订阅＋独立认证服务','Stripe subscriptions + a separate auth service')],['other',t('其他组合','Another setup')],['unknown',t('尚未确认','Not sure')]]],['hours',t('最近一次验收的团队工时','Team time for the last review'),[['under1',t('少于 1 小时','Under 1 hour')],['1to3',t('1–3 小时','1–3 hours')],['over3',t('超过 3 小时','Over 3 hours')],['unknown',t('不知道或没有做过','Unknown or not done')]]],['budget',t('对下方 $299 范围的预算意向','Budget response to the $299 scope below'),[['299',t('范围合适，愿讨论按 $299 采购','Scope fits; willing to discuss buying at $299')],['discuss',t('有任务，但价格或范围需调整','Have a task; price or scope needs discussion')],['no',t('目前不会付费','Would not pay at present')]]]];
 const checks=[t('未付款账户不能访问付费功能','Unpaid accounts cannot use paid features'),t('付款后按约定时限开通权益','Payment grants the right access within the agreed time'),t('不同账号和租户的权限隔离','Accounts and tenants keep access separate'),t('升级或降级按确认规则生效','Plan changes follow agreed rules'),t('取消订阅在正确日期停止权益','Cancellation ends access at the agreed date'),t('续费失败和退款遵守业务政策','Failed renewals and refunds follow the business policy')];
 const FAQ=[{q:t('清单是否会测试我的应用？','Does the checklist test my app?'),a:t('不会。它只帮助准备任务，全部勾选也不表示应用通过验收。我们不连接你的应用或收取测试密钥。','No. It prepares the task. Checking every item does not certify your app. This page does not connect to your app or collect test credentials.')},{q:t('提交后会被收费或自动预约吗？','Will submitting charge me or reserve a slot?'),a:t('不会。当前收集试点申请，不收款。只有双方确认环境、业务规则、报价、退款安排与交付排期后，才可能单独下单。','No. This is a pilot application with no payment. A separate order requires agreement on the environment, business rules, price, refunds and delivery schedule.')},{q:t('你们保证没有漏洞吗？','Do you guarantee there are no bugs?'),a:t('不保证。拟议交付是约定范围内的执行证据、缺陷记录和一次复测，不是安全认证、修复服务或全面测试。','No. The proposed deliverable is evidence for the agreed scope, defect records and one retest. It is not security certification, bug fixing or comprehensive testing.')}];
 const body=`${railOf()}<link rel="stylesheet" href="${asset}/release-pilot.css"><main class="stage studio-main" id="main-content" data-release-pilot data-lang="${zh?'zh':'en'}"><nav class="crumb"><a href="${BASE}/">${NAME}</a><i>/</i><a href="${BASE}/studio/">${t('自研工具','Built by BPJ')}</a><i>/</i><span>${t('收费应用验收','Paid app review')}</span></nav>
 <header class="studio-hero"><h1>${title}</h1><p>${desc}</p><div class="studio-actions"><a class="studio-button primary" href="#checklist">${t('准备检查清单','Prepare a checklist')}</a><a class="studio-button" href="#offer">${t('查看范围与价格','View scope and price')}</a></div></header>
 <section class="pilot-report"><h2>${t('一份报告，应说明什么','What a useful report should show')}</h2><p>${t('以下为虚构示例，不是客户结果。','Illustrative example only. This is not a customer result.')}</p><dl><dt>${t('场景','Scenario')}</dt><dd>${t('订阅取消，到期撤销高级功能','Cancel a subscription; revoke premium access at expiry')}</dd><dt>${t('预期规则','Expected rule')}</dt><dd>${t('示例政策：账期结束后 60 秒内撤权','Example policy: revoke within 60 seconds after the billing period ends')}</dd><dt>${t('观察结果','Observation')}</dt><dd>${t('示例中，120 秒后仍可访问高级功能','In this example, premium access still works after 120 seconds')}</dd><dt>${t('结论与证据','Finding and evidence')}</dt><dd>${t('未通过。交付应包含环境与版本、对象 ID、时间戳、复现步骤及复测结论。','Failed. Delivery should include environment and revision, object IDs, timestamps, reproduction steps and the retest finding.')}</dd></dl></section>
 <section class="quote-step" id="checklist"><h2>${t('先明确你要验证的规则','Start with the rules you need checked')}</h2><p>${t('勾选需要纳入的场景，导出准备清单。这里不执行测试，也不给通过分数。','Select the scenarios you need and export a preparation checklist. This page does not run tests or give a passing score.')}</p><div class="pilot-checks">${checks.map((c,i)=>`<label><input type="checkbox" name="check" value="${i}"> ${c}</label>`).join('')}</div><button class="studio-button" type="button" id="pilot-export">${t('下载准备清单','Download checklist')}</button><p id="check-status" role="status"></p></section>
 <section class="quote-step" id="offer"><h2>${t('拟议试点：一个应用，$299','Proposed pilot: one app, $299')}</h2><p><strong>${t('当前仅接受申请，不收款。','Applications only. No payment is collected.')}</strong> ${t('拟议美元单次价格；最终税费与总价会在任何付款前单独确认。','Proposed one-time USD price; any applicable tax and the final total will be confirmed before payment.')}</p><ul><li>${t('一个隔离测试环境，一套 Stripe 订阅、一种认证方式','One isolated test environment, one Stripe subscription setup and one auth method')}</li><li>${t('最多两个付费套餐、12 个确认用例、Chromium 浏览器','Up to two paid plans, 12 agreed cases and Chromium')}</li><li>${t('规则表、执行证据、可复现缺陷和一个固定版本的复测','Rule sheet, execution evidence, reproducible defects and one fixed-revision retest')}</li></ul><p>${t('不含生产环境操作、代码修复、多浏览器、安全认证或持续监控。测试开始前必须确认具体排期。','Excludes production operations, code fixes, multiple browsers, security certification and ongoing monitoring. The delivery schedule must be agreed before testing.')}</p><p>${t('适合已有收费应用、近期要交付变更的工作室。若现有方法已经足够，不需要换。','For studios with existing paid apps and an upcoming change. If your current process already meets the need, keep it.')}</p><a href="#application" class="studio-button primary" id="apply-open">${t('提交任务与预算意向','Describe the task and budget')}</a></section>
 <section class="quote-step" id="application"><h2>${t('申请讨论一个真实任务','Request a discussion about a real task')}</h2><p>${t('申请窗口至 2026 年 10 月 10 日（北京时间）。申请不保证接单或回复时限。请勿提交应用密钥、客户数据或支付资料。','Applications close on 10 October 2026 (Asia/Shanghai). Submission does not guarantee acceptance or a response time. Do not submit credentials, customer data or payment details.')}</p><form id="pilot-form"><div class="quote-grid">${fields.map(([key,label,opts])=>`<label>${label}<select name="${key}" required><option value="">${t('请选择','Choose one')}</option>${opts.map(([v,n])=>`<option value="${v}">${n}</option>`).join('')}</select></label>`).join('')}<label>${t('用于本次申请的联系邮箱','Contact email for this application')}<input name="email" type="email" maxlength="254" autocomplete="email" required></label></div><label class="pilot-consent"><input name="consent" type="checkbox" required> ${t('我同意 BPJ 保存上述申请并就此任务联系我；这不是邮件订阅或订单。','I agree that BPJ may store this application and contact me about this task. This is not a newsletter subscription or an order.')}</label><button class="studio-button primary" type="submit">${t('提交试点申请','Submit pilot application')}</button><p id="pilot-status" role="status" aria-live="polite"></p></form><div id="pilot-receipt" hidden><p>${t('已保存。请下载回执，凭回执可撤回申请。回执不包含邮箱；待确认的提交可能暂存在当前标签页。','Saved. Download the receipt to withdraw this application later. The receipt does not contain your email; a pending submission may be retained in this tab.')}</p><button type="button" class="studio-button" id="receipt-download">${t('下载申请回执','Download receipt')}</button><button type="button" class="studio-button" id="pilot-withdraw">${t('撤回本次申请','Withdraw application')}</button></div><details><summary>${t('使用已有回执撤回申请','Withdraw using an existing receipt')}</summary><label>${t('选择你下载的 JSON 回执','Select your downloaded JSON receipt')} <input id="receipt-import" type="file" accept="application/json,.json"></label></details></section>
 <section class="studio-info" id="pilot-privacy"><h2>${t('数据与使用边界','Data and scope')}</h2><p>${t('勾选内容留在本机，只有点击提交才发送邮箱与申请选项。我们记录化名会话事件（提交申请后可关联到邮箱）以了解页面、价格与申请步骤，不把它当真人数或营收。回执存于当前标签页会话，可下载保留。','Checklist selections stay on your device. Your email and task choices are sent only when you submit. Pseudonymous session events (linkable to your email after applying) measure the page, price and application steps; they are not verified people or revenue. Your receipt stays in this tab session and can be downloaded.')}</p><p>${t('申请与事件使用 30 天评估窗口；到期后在下一次成功的接口访问或每日巡检时清理，可能晚于第 30 天。不会自动加入营销名单。为限制重复提交，保存按日加密散列的网络标识，不保存原始 IP。撤回后删除申请，匿名事件可能保留至过期。','Applications and events have a 30-day evaluation window. Expired records are deleted on the next successful API access or daily check, which may occur after day 30. No automatic marketing signup. A daily keyed network digest limits repeat submissions; raw IP addresses are not stored. Withdrawal deletes the application; anonymous events may remain until expiry.')}</p>${FAQ.map(f=>`<h3>${f.q}</h3><p>${f.a}</p>`).join('')}<p><a href="#pilot-privacy">${t('站点隐私说明','Site privacy notice')}</a></p></section></main><script type="module" src="${asset}/release-pilot.mjs"></script>`;
 write('studio/release-check.html',layout({title:title+' - BPJ',description:desc,path,body,wide:true,privacyNotice:t('本页申请邮箱与任务选项仅用于试点评估和联系；可凭回执撤回。完整数据说明见页面正文。','Application emails and task choices are used for pilot evaluation and contact; withdraw with your receipt. See the data notice on this page.'),schema:[crumbLd([{name:NAME,url:BASE+'/'},{name:t('自研工具','Built by BPJ'),url:BASE+'/studio/'},{name:title,url:BASE+path}]),faqLd(FAQ)]}));pushPage(BASE+path+'.html','0.8');
}
